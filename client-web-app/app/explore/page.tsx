import { ExplorePage } from '@/src/features/explore';

export const metadata = {
  title: 'Explore - MemoryVault',
  description: 'Discover stories, time capsules, and memories shared by the MemoryVault community',
};

export default function Explore() {
  return <ExplorePage />;
}

