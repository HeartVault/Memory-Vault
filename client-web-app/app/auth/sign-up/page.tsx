import { SignupPage } from '@/src/features/auth';

export const metadata = {
  title: 'Sign Up - MemoryVault',
  description: 'Create your MemoryVault account',
};

export default function SignUpPage() {
  return <SignupPage />;
}

