import { LoginPage } from '@/src/features/auth';

export const metadata = {
  title: 'Sign In - MemoryVault',
  description: 'Sign in to your MemoryVault account',
};

export default function SignInPage() {
  return <LoginPage />;
}

